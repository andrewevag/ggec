int main(){
	//mashup
	y = 2 == 2 ? 10 : 12+2 >= 90 && 38 || true;
	// precedences
	
	+func()++ * &a[3]-- / -++main() % --*b[64] + new int*++ / delete x++;
	delete x++;
	x %= 12 /= 13 ? 10 : 9, x += 3;

	/*
	 * 	Comma tests
	 */
	x*2, y, z;
	main(y, z, w, x), y(3), 32++;
	
}