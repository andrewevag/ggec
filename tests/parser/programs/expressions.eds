int main(){
	//mashup
	
	x;
	(x);
	true;
	false;
	NULL;
	32;
	'c';
	12.63;
	"hello ma boys\r\t";
	main();
	main("hello", 3+2);
	a[32] = 12;
	+-!*&x;
	3+2%8*7-12/x >= 21 && 12;
	++x;
	y++;
	++x--;
	x %= 2 /= 12;
	y += 13;
	k -= y;
	y *= 132;
	(int) 'c';
	new int* [2];
	x = new int;
	delete x;
	x, y * 2, 3;
	y = 2 == 2 ? 10 : 12+2 >= 90 && 38 || true;
	// precedences
	
	+func()++ * &a[3]-- / -++main() % --*b[64] + new int*++ / delete x++;
	delete x++;
	x %= 12 /= 13 ? 10 : 9, x += 3;

	/*
	 * 	Comma tests
	 */
	x*2, y, z;
	main(y, z, w, x), y(3), 32++;
}